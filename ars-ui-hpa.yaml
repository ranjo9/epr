{{- if and .Values.arsUi.enabled .Values.arsUi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ars-ui.fullname" . }}
  labels:
    {{- include "ars-ui.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ars-ui.fullname" . }}
  minReplicas: {{ .Values.arsUi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.arsUi.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.arsUi.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.arsUi.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.arsUi.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.arsUi.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
