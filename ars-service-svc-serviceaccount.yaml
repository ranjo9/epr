{{- if and .Values.arsServiceSvc.enabled .Values.arsService.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ars-service-svc.serviceAccountName" . }}
  labels:
    {{- include "ars-service-svc.labels" . | nindent 4 }}
  {{- with .Values.arsService.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "ars-service-svc.serviceAccountName" . }}
rules:
  - apiGroups: ["", "extensions", "apps"]
    resources: ["configmaps", "pods", "services", "endpoints", "secrets"]
    verbs: ["get", "list", "watch"]

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "ars-service-svc.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
subjects:
- kind: ServiceAccount
  name: {{ include "ars-service-svc.serviceAccountName" . }}
  apiGroup: ""
roleRef:
  kind: Role
  name: {{ include "ars-service-svc.serviceAccountName" . }}
  apiGroup: rbac.authorization.k8s.io
{{- end }}
