# # audit-query-service — slv environment properties
# # Loaded via ConfigMap into the container at runtime

# server.port=8080
# server.ssl.enabled=true
# server.ssl.key-store=/tls/keystore.p12
# server.ssl.key-store-type=PKCS12
# server.ssl.key-store-password=${ssl-key-store-password}

# # --- Datasource (Azure SQL) ---
# spring.datasource.url=jdbc:oracle:thin:@//csdbx02dev1e2cz.nhsrx.com:1521/nhindo.pdxinc.com;databaseName=arsdb;encrypt=true;trustServerCertificate=false;loginTimeout=30;
# spring.datasource.username=${ARS_DB_USERNAME}
# spring.datasource.password=${ARS_DB_PASSWORD}
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# # --- Flyway ---
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration


# # --- Actuator ---
# management.endpoints.web.exposure.include=health,info

# # --- OAuth2 / JWT ---
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://login.microsoftonline.com/REPLACE_ME/v2.0
